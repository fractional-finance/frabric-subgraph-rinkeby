{
  "name": "frabric-subgraph",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "test": "graph test",
    "deploy": "graph deploy fractional-finance/frabric-subgraph --ipfs https://api.thegraph.com/ipfs/ --node https://api.thegraph.com/deploy/ --access-token $ACCESS_TOKEN",
    "create-local": "graph create --node http://localhost:8020/ the-frabric",
    "remove-local": "graph remove --node http://localhost:8020/ the-frabric",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 the-frabric"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "^0.40.0",
    "@graphprotocol/graph-ts": "^0.27.0",
    "matchstick-as": "^0.5.1"
  }
}
